name: testando
on: [push]

jobs:
  build:
    name: testando
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install
      - run: |
          apt install cppcheck
      
      - name: run check 1
      - run: |
          cppcheck test_correct.c


