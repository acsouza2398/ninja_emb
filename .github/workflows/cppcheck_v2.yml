name: cppcheck

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: test-7
      run: cppcheck 1-PIO-IO/src -i1-PIO-IO/src/ASF -i1-PIO-IO/src/config
